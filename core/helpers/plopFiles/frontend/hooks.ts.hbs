import { useEffect } from 'react';
import { {{pascalCase name}}Props, {{pascalCase name}}Handlers } from './{{camelCase name}}.types';
import { RootState, useDispatch, useSelector } from "@/core/redux";
import {{pascalCase name}}Thunk from "@/core/redux/{{camelCase name}}";

export const use{{pascalCase name}} = (props: {{pascalCase name}}Props) => {
  const dispatch = useDispatch();

   const {{lowerCase name}} = useSelector((state: RootState) => state.{{camelCase name}}.data);

  // Define handlers if necessary
  const handlers: {{pascalCase name}}Handlers = {};

  useEffect(() => {
    // Add your side effects here
    dispatch(
      {{pascalCase name}}Thunk.fetch{{pascalCase name}}({
        page: 1,
        limit: 10,
      })
    );
  }, []);

  return {
    handlers,
  };
};
